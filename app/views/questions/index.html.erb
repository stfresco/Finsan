<div class="container chatbot">
  <h1>Asistente financiero</h1>
  <div id="questions">
    <%= render @questions %>
  </div>

  <%= render "form", question: @question %>

  <div class="text-center mt-4 mb-5">
    <%= link_to 'Volver a Inicio', home_path, class: 'btn btn-primary' %>
  </div>
</div>
