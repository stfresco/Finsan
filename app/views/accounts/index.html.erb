<div class="text-center mb-5">
  <h1 class="display-4 fw-bold">¡Tus Cuentas! 😎</h1>
  <p class="lead">Administra tus cuentas de forma sencilla y rápida.</p>
</div>

<div class="row g-4">
  <% @accounts.each do |account| %>
    <div class="col-lg-3 col-md-4 col-sm-6">
      <div class="card shadow-sm h-100">
        <div class="card-body text-center">
          <h5 class="card-title text-primary fw-bold"><%= account.bank %></h5>
          <h6 class="card-subtitle text-muted mb-3">
            <%= account.account_type.capitalize %>
          </h6>
          <p class="text-success fs-5 fw-bold mb-4">
            <%= number_to_currency(account.balance.total_amount, unit: "$") %>
          </p>
          <%= link_to "Detalles", account_path(account), class: "btn btn-primary btn-sm" %>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="mt-5 text-center">
  <%= link_to content_tag(:i, "", class: "fa-solid fa-arrow-left me-1") + " Volver",
              login_path,
              class: "btn btn-outline-secondary btn-sm me-2" %>
  <%= link_to content_tag(:i, "", class: "fa-solid fa-plus me-1") + " Agregar una cuenta",
              new_account_path,
              class: "btn btn-primary btn-sm" %>
</div>
